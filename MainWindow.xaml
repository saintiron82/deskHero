<Window x:Class="DeskWarrior.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DeskWarrior.ViewModels"
        Title="DeskWarrior"
        Width="280"
        Height="285"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Left="100"
        Top="100"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        KeyDown="Window_KeyDown">

    <Grid>
        <!-- 배경 이미지 (윈도우 전체, 가장 뒤) -->
        <Image x:Name="BackgroundImage"
               Source="pack://application:,,,/Assets/Images/bg_floor_sparse_pilot_v2.png"
               Stretch="UniformToFill"
               Margin="3"
               Panel.ZIndex="0"
               IsHitTestVisible="False"/>


        <!-- 메인 컨텐츠 -->
        <Border x:Name="MainBackgroundBorder" Background="#CC2A2A2A" CornerRadius="10" Margin="3" Panel.ZIndex="1">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="32"/>
                    <RowDefinition Height="*" MinHeight="50"/>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0: 재화 + 타이머 + X 버튼 -->
                <Border x:Name="GoldInfoBarTop" Grid.Row="0"
                        Background="#44000000" CornerRadius="5" Padding="4,2"
                        BorderBrush="#00CED1" BorderThickness="1"
                        VerticalAlignment="Stretch"
                        Margin="0"
                        Cursor="Hand"
                        MouseLeftButtonDown="InfoBar_MouseLeftButtonDown"
                        ToolTip="클릭하여 모드 전환 (F1)">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 골드 -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="pack://application:,,,/Assets/Images/UI/gold.png" Width="16" Height="16" VerticalAlignment="Center" Margin="0,2,3,0"/>
                            <TextBlock x:Name="GoldTextTop" Text="0" FontSize="12" Foreground="#FFD700" FontWeight="Bold" VerticalAlignment="Center" MinWidth="32"/>
                        </StackPanel>

                        <!-- 크리스털 -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                            <Image Source="pack://application:,,,/Assets/Images/UI/crystal.png" Width="16" Height="16" VerticalAlignment="Center" Margin="0,2,3,0">
                                <Image.Effect>
                                    <DropShadowEffect Color="#00CED1" ShadowDepth="0" BlurRadius="4" Opacity="0.5"/>
                                </Image.Effect>
                            </Image>
                            <TextBlock x:Name="CrystalTextTop" Text="0" FontSize="12" Foreground="#00CED1" FontWeight="Bold" VerticalAlignment="Center" MinWidth="24">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="#00CED1" ShadowDepth="0" BlurRadius="4" Opacity="0.5"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>

                        <!-- 타이머 -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                            <Image Source="pack://application:,,,/Assets/Images/UI/Icon_Timer.png" Width="14" Height="14" VerticalAlignment="Center" Margin="0,2,3,0"/>
                            <TextBlock x:Name="TimerText" Text="30" FontSize="12" FontWeight="Bold" Foreground="#87CEEB" VerticalAlignment="Center" MinWidth="20" TextAlignment="Left"/>
                        </StackPanel>

                        <!-- 우측: X 버튼 -->
                        <Border x:Name="ExitButtonBorderInline" Grid.Column="4"
                                Background="#662222"
                                BorderBrush="#FF4444" BorderThickness="1"
                                CornerRadius="3"
                                Width="18" Height="18"
                                Cursor="Hand"
                                Visibility="Visible"
                                ToolTip="종료"
                                MouseLeftButtonDown="ExitButton_MouseLeftButtonDown">
                            <TextBlock Text="✕" FontSize="10" Foreground="#FF8888"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- Row 1: 적 정보 -->
                <Border x:Name="EnemyInfoBorder" Grid.Row="1"
                        Background="#88000000" CornerRadius="4" Padding="6,4"
                        VerticalAlignment="Stretch" Margin="0,2,0,0"
                        Panel.ZIndex="1">
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="LevelText" Text="Lv.1" FontSize="12" Foreground="White" FontWeight="Bold"/>
                            <TextBlock Text="{Binding MonsterName}" FontSize="12" Foreground="#88ff88" Margin="5,0,0,0" FontWeight="SemiBold"/>
                            <TextBlock x:Name="MonsterEmoji" Text=" 👹" FontSize="12"/>
                            <TextBlock x:Name="MaxLevelText" Text=" (Best: 1)" FontSize="8" Foreground="#AAA" VerticalAlignment="Bottom" Margin="4,0,0,0" Visibility="Collapsed"/>
                        </StackPanel>

                        <!-- HP 바 -->
                        <Grid Grid.Column="1" Height="8" Margin="8,0,0,0" VerticalAlignment="Center">
                            <Border Background="#333" CornerRadius="3"/>
                            <Border x:Name="HpBar" Background="#00FF00" CornerRadius="3" HorizontalAlignment="Left" Width="80"/>
                        </Grid>
                        <TextBlock x:Name="HpText" Text="100/100" FontSize="9" Foreground="#AAA" HorizontalAlignment="Left" Visibility="Collapsed"/>
                    </Grid>
                </Border>

                <!-- Row 2: 배틀 필드 (몬스터, 히어로) -->
                <Grid Grid.Row="2" Panel.ZIndex="1">
                    <!-- 적 몬스터 (우측 상단) -->
                    <Image x:Name="MonsterImage"
                           Source="pack://application:,,,/Assets/Images/monster.png"
                           Width="80" Height="80"
                           Stretch="Uniform"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Top"
                           Margin="0,-10,10,0"
                           RenderTransformOrigin="0.5,0.5"
                           Panel.ZIndex="2"
                           MouseLeftButtonDown="GameElement_MouseLeftButtonDown">
                        <Image.RenderTransform>
                            <TranslateTransform x:Name="MonsterShakeTransform"/>
                        </Image.RenderTransform>
                    </Image>

                    <!-- 데미지 팝업 영역 -->
                    <Canvas x:Name="DamagePopupCanvas"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Width="90" Height="90"
                            Margin="0,5,5,0"
                            Panel.ZIndex="10"
                            IsHitTestVisible="False"/>

                    <!-- 플레이어 히어로 (좌측 하단) -->
                    <Image x:Name="HeroImage"
                           Source="pack://application:,,,/Assets/Images/hero.png"
                           Width="90" Height="90"
                           Stretch="Uniform"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Bottom"
                           Margin="10,0,0,-35"
                           Panel.ZIndex="3"
                           MouseLeftButtonDown="GameElement_MouseLeftButtonDown"/>
                </Grid>

                <!-- Row 3: 공격력 정보바 (항상 표시) -->
                <Border x:Name="PowerInfoBar" Grid.Row="3"
                        Background="#44000000" CornerRadius="5"
                        Padding="8,6" Margin="0,4,0,0"
                        VerticalAlignment="Stretch"
                        Visibility="Visible">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock x:Name="KeyboardPowerText"
                                   Text="⌨️ Atk: 1" FontSize="10" Foreground="#00CED1"
                                   VerticalAlignment="Center"/>
                        <Border Width="1" Height="12" Background="#444" Margin="12,0"/>
                        <TextBlock x:Name="MousePowerText"
                                   Text="🖱️ Atk: 1" FontSize="10" Foreground="#E8D03F"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Row 4: 업그레이드 버튼 패널 -->
                <Border x:Name="UpgradePanel" Grid.Row="4"
                        Background="#88000000" CornerRadius="4"
                        Margin="0,2,0,0" Padding="2"
                        Visibility="Visible"
                        Panel.ZIndex="20"
                        PreviewMouseLeftButtonDown="UpgradePanel_PreviewMouseLeftButtonDown">
                    <UniformGrid Columns="2" Rows="1">
                        <!-- 키보드 공격력 -->
                        <Button x:Name="UpgradeKeyboardBtn"
                                Click="UpgradeKeyboard_Click"
                                Style="{StaticResource StatUpgradeButton}"
                                Margin="1" Height="24"
                                ToolTip="키보드 공격력 증가">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock x:Name="KeyboardIconText" Text="⌨" FontSize="14" VerticalAlignment="Center" Margin="0,0,3,0" Foreground="#E8D03F" FontWeight="Bold"/>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock x:Name="KeyboardNameText" Text="Keyboard" FontSize="7" Foreground="White" Margin="0,1,0,0" Visibility="Collapsed"/>
                                    <TextBlock x:Name="KeyboardLevelText" Text="Lv.0" FontSize="10" Foreground="#E8D03F" FontWeight="Black"/>
                                    <StackPanel x:Name="KeyboardCostPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,1,0,0" Visibility="Collapsed">
                                        <Image Source="pack://application:,,,/Assets/Images/UI/gold.png" Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,2,0"/>
                                        <TextBlock x:Name="KeyboardCostText" Text="100" FontSize="7" FontWeight="Bold"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Button>

                        <!-- 마우스 공격력 -->
                        <Button x:Name="UpgradeMouseBtn"
                                Click="UpgradeMouse_Click"
                                Style="{StaticResource StatUpgradeButton}"
                                Margin="1" Height="24"
                                ToolTip="마우스 공격력 증가">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock x:Name="MouseIconText" Text="🖰" FontSize="14" VerticalAlignment="Center" Margin="0,0,3,0" Foreground="#E8D03F" FontWeight="Bold"/>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock x:Name="MouseNameText" Text="Mouse" FontSize="7" Foreground="White" Margin="0,1,0,0" Visibility="Collapsed"/>
                                    <TextBlock x:Name="MouseLevelText" Text="Lv.0" FontSize="10" Foreground="#E8D03F" FontWeight="Black"/>
                                    <StackPanel x:Name="MouseCostPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,1,0,0" Visibility="Collapsed">
                                        <Image Source="pack://application:,,,/Assets/Images/UI/gold.png" Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,2,0"/>
                                        <TextBlock x:Name="MouseCostText" Text="100" FontSize="7" FontWeight="Bold"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Button>
                    </UniformGrid>
                </Border>

                <!-- Row 5: 유틸리티 버튼 패널 (통계, 상점, 설정) -->
                <Border x:Name="UtilityPanel" Grid.Row="5"
                        Background="#88000000" CornerRadius="4"
                        Margin="0,2,0,0" Padding="2"
                        Visibility="Visible"
                        Panel.ZIndex="20">
                    <UniformGrid x:Name="UtilityButtonsGrid" Columns="3" Rows="1">
                        <!-- 통계 버튼 -->
                        <Button x:Name="StatsBtn"
                                Click="StatsButton_Click"
                                Style="{StaticResource SettingsButton}"
                                Margin="1" Height="24"
                                ToolTip="통계 보기">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="📊" FontSize="12" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                <TextBlock Text="통계" FontSize="10" VerticalAlignment="Center" Foreground="White"/>
                            </StackPanel>
                        </Button>

                        <!-- 영구 상점 버튼 -->
                        <Button x:Name="PermanentShopBtn"
                                Click="PermanentShopButton_Click"
                                Style="{StaticResource SettingsButton}"
                                Margin="1" Height="24"
                                ToolTip="상점">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/Images/UI/crystal.png" Width="12" Height="12" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                <TextBlock Text="상점" FontSize="10" VerticalAlignment="Center" Foreground="White"/>
                            </StackPanel>
                        </Button>

                        <!-- 설정 버튼 -->
                        <Button x:Name="SettingsBtn"
                                Click="SettingsButton_Click"
                                Style="{StaticResource SettingsButton}"
                                Margin="1" Height="24"
                                ToolTip="설정 열기">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="⚙️" FontSize="12" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                <TextBlock Text="설정" FontSize="10" VerticalAlignment="Center" Foreground="White"/>
                            </StackPanel>
                        </Button>
                    </UniformGrid>
                </Border>
            </Grid>
        </Border>

        <!-- 디버그 표시 -->
        <TextBlock x:Name="DebugText" Text="" Foreground="#888" FontSize="8"
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5"
                   Visibility="Collapsed"/>

        <!-- 입력 횟수 (숨김, 통계용) -->
        <TextBlock x:Name="InputCountText" Text="0" Visibility="Collapsed"/>

        <!-- Game Over Overlay -->
        <Border x:Name="GameOverOverlay" Visibility="Collapsed" Panel.ZIndex="200"
                Background="#CC000000" MouseLeftButtonDown="Window_MouseLeftButtonDown">
            <Border Background="#3A3A3A"
                    BorderBrush="#00CED1"
                    BorderThickness="2"
                    CornerRadius="8"
                    Padding="16,12"
                    Margin="8"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch">
                <Border.Effect>
                    <DropShadowEffect Color="#00CED1" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel>
                    <!-- 제목 -->
                    <TextBlock Text="⚰️ GAME OVER"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="#FF4444"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,6">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Red" BlurRadius="8" ShadowDepth="0"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <!-- 게임 오버 메시지 -->
                    <TextBlock x:Name="GameOverMessageText"
                               Text="..."
                               Foreground="#AAA"
                               FontSize="8"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               Margin="0,0,0,8"
                               TextWrapping="Wrap"
                               MaxWidth="260"/>

                    <!-- 세션 통계 -->
                    <TextBlock Text="📊 세션 결과"
                               FontSize="12"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,4"/>
                    <Border Background="#222" BorderBrush="#444" BorderThickness="1" CornerRadius="5" Padding="10,6" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 레벨 -->
                            <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,8,4">
                                <TextBlock Text="레벨" FontSize="8" Foreground="#888"/>
                                <TextBlock x:Name="ReportLevelText" Text="0" FontSize="11" Foreground="White" FontWeight="Bold"/>
                            </StackPanel>

                            <!-- 몬스터 -->
                            <StackPanel Grid.Column="1" Grid.Row="0" Margin="8,0,0,4">
                                <TextBlock Text="몬스터" FontSize="8" Foreground="#888"/>
                                <TextBlock FontSize="11" Foreground="White" FontWeight="Bold">
                                    <Run x:Name="ReportKillsText" Text="0"/>
                                    <Run Text="마리" FontSize="9" Foreground="#AAA"/>
                                </TextBlock>
                            </StackPanel>

                            <!-- 데미지 -->
                            <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,8,0">
                                <TextBlock Text="데미지" FontSize="8" Foreground="#888"/>
                                <TextBlock x:Name="ReportDamageText" Text="0" FontSize="11" Foreground="#FF8888" FontWeight="Bold"/>
                            </StackPanel>

                            <!-- 골드 -->
                            <StackPanel Grid.Column="1" Grid.Row="1" Margin="8,0,0,0">
                                <TextBlock Text="골드" FontSize="8" Foreground="#888"/>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="pack://application:,,,/Assets/Images/UI/gold.png" Width="11" Height="11" VerticalAlignment="Center" Margin="0,0,2,0"/>
                                    <TextBlock x:Name="ReportGoldText" Text="0" FontSize="11" Foreground="#FFD700" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- 크리스탈 획득 (간단 표시) -->
                    <Border Background="#2A2A2A"
                            BorderBrush="#00CED1"
                            BorderThickness="1"
                            CornerRadius="5"
                            Padding="10,8"
                            Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Image Source="pack://application:,,,/Assets/Images/UI/crystal.png" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock FontSize="14" FontWeight="Bold" Foreground="#00CED1" VerticalAlignment="Center">
                                <Run Text="+"/>
                                <Run x:Name="TotalCrystalsEarnedRun" Text="0"/>
                            </TextBlock>
                            <TextBlock Text=" (보유: " FontSize="10" Foreground="#666" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            <TextBlock x:Name="CurrentCrystalBalanceRun" Text="0" FontSize="10" Foreground="#00CED1" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBlock Text=")" FontSize="10" Foreground="#666" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- 숨김 요소 (코드 호환성) -->
                    <TextBlock x:Name="SessionGoldRun" Visibility="Collapsed"/>
                    <TextBlock x:Name="ConvertedCrystalsRun" Visibility="Collapsed"/>
                    <StackPanel x:Name="BossDropLine" Visibility="Collapsed">
                        <TextBlock x:Name="BossDropCrystalsRun"/>
                    </StackPanel>
                    <StackPanel x:Name="AchievementRewardLine" Visibility="Collapsed">
                        <TextBlock x:Name="AchievementCrystalsRun"/>
                    </StackPanel>

                    <!-- 액션 버튼 -->
                    <Grid HorizontalAlignment="Center" Margin="0,4,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button x:Name="ShopButton"
                                Grid.Column="0"
                                Content="🛒 상점 (S)"
                                Click="ShopButton_Click"
                                Width="100"
                                Height="32"
                                Margin="0,0,6,0"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="11"
                                FontWeight="Bold">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#00CED1" Offset="0"/>
                                    <GradientStop Color="#1DD1D1" Offset="1"/>
                                </LinearGradientBrush>
                            </Button.Background>
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                            <Button.Effect>
                                <DropShadowEffect Color="#00CED1" BlurRadius="12" ShadowDepth="2" Opacity="0.4"/>
                            </Button.Effect>
                        </Button>
                        <Button x:Name="CloseOverlayButton"
                                Grid.Column="1"
                                Content="▶️ 게임 (SPACE)"
                                Click="CloseOverlayButton_Click"
                                Width="100"
                                Height="32"
                                Margin="6,0,0,0"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="11"
                                FontWeight="Bold">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#E8D03F" Offset="0"/>
                                    <GradientStop Color="#B89F2C" Offset="1"/>
                                </LinearGradientBrush>
                            </Button.Background>
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                            <Button.Effect>
                                <DropShadowEffect Color="#E8D03F" BlurRadius="12" ShadowDepth="2" Opacity="0.4"/>
                            </Button.Effect>
                        </Button>
                    </Grid>

                    <!-- 자동 닫기 카운트다운 -->
                    <TextBlock x:Name="AutoCloseCountdownText"
                               Text="10초 후 닫힘"
                               Foreground="#666"
                               FontSize="8"
                               HorizontalAlignment="Center"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
