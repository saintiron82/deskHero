<Window x:Class="DeskWarrior.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DeskWarrior.ViewModels"
        Title="DeskWarrior"
        Width="280"
        Height="250"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Left="100"
        Top="100"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Grid>
        <!-- 관리 모드 표시 테두리 -->
        <Border x:Name="ManageModeBorder"
                BorderBrush="#FFD700"
                BorderThickness="3"
                CornerRadius="10"
                Visibility="Collapsed"
                Panel.ZIndex="100"/>

        <!-- 모드 전환 버튼 (호버 시 표시) -->
        <Border x:Name="ModeToggleBorder"
                Background="#DD222233"
                BorderBrush="#666" BorderThickness="1"
                CornerRadius="6"
                Width="28" Height="28"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="8,8,0,0"
                Cursor="Hand"
                Opacity="0"
                ToolTip="관리 모드로 전환 (F1)"
                MouseLeftButtonDown="ModeToggle_MouseLeftButtonDown"
                Panel.ZIndex="150">
            <TextBlock x:Name="ModeIcon"
                       Text="👁️"
                       FontSize="16"
                       Foreground="#00CED1"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <!-- 종료 버튼 (관리 모드에서만 표시) -->
        <Border x:Name="ExitButtonBorder"
                Background="#662222"
                BorderBrush="#FF4444" BorderThickness="1"
                CornerRadius="6"
                Width="28" Height="28"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,8,8,0"
                Cursor="Hand"
                Visibility="Collapsed"
                ToolTip="종료"
                MouseLeftButtonDown="ExitButton_MouseLeftButtonDown"
                Panel.ZIndex="150">
            <TextBlock Text="❌"
                       FontSize="14"
                       Foreground="#FF8888"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <!-- 메인 컨텐츠 -->
        <Border x:Name="MainBackgroundBorder" Background="#EE1a1a2e" CornerRadius="10" Margin="3">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0: 적 정보 (우측 정렬) -->
                <Border x:Name="EnemyInfoBorder" Grid.Row="0"
                        Background="#44000000" CornerRadius="8" Padding="8,4"
                        HorizontalAlignment="Right">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="LevelText" Text="Lv.1" FontSize="12" Foreground="White" FontWeight="Bold"/>
                            <TextBlock Text="{Binding MonsterName}" FontSize="12" Foreground="#88ff88" Margin="5,0,0,0" FontWeight="SemiBold"/>
                            <TextBlock x:Name="MaxLevelText" Text=" (Best: 1)" FontSize="8" Foreground="#AAA" VerticalAlignment="Bottom" Margin="4,0,0,0" Visibility="Collapsed"/>
                            <TextBlock x:Name="MonsterEmoji" Text=" 👹" FontSize="12"/>
                        </StackPanel>
                        <!-- HP 바 -->
                        <Grid Height="6" Width="80" Margin="0,3,0,0">
                            <Border Background="#333" CornerRadius="3"/>
                            <Border x:Name="HpBar" Background="#00FF00" CornerRadius="3" HorizontalAlignment="Left" Width="80"/>
                        </Grid>
                        <TextBlock x:Name="HpText" Text="100/100" FontSize="9" Foreground="#AAA" HorizontalAlignment="Right" Visibility="Collapsed"/>
                    </StackPanel>
                </Border>

                <!-- Row 0: 타이머 (좌측 정렬) - UX 개선: 헤더 영역에 배치하여 시인성 확보 -->
                <Border x:Name="TimerInfoBar" Grid.Row="0"
                        Background="#44000000" CornerRadius="8" Padding="10,4"
                        HorizontalAlignment="Left" VerticalAlignment="Top"
                        Margin="0,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⏱️" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock Text=" " FontSize="12"/>
                        <TextBlock x:Name="TimerText" Text="30" FontSize="14" FontWeight="Bold" Foreground="#87CEEB" VerticalAlignment="Center" Width="25" TextAlignment="Center"/>
                    </StackPanel>
                </Border>

                    <!-- 배틀 필드 -->
                <Grid Grid.Row="1">
                    <!-- 배경 이미지 (가장 뒤) -->
                    <Image Source="pack://application:,,,/Assets/Images/bg_floor_sparse_pilot_v2.png"
                           Stretch="UniformToFill"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Top"
                           Height="150"
                           Margin="0,0,0,0"
                           Panel.ZIndex="1"/>

                    <!-- 적 몬스터 (우상단) -->
                    <Image x:Name="MonsterImage"
                           Source="pack://application:,,,/Assets/Images/monster.png"
                           Width="100" Height="100"
                           Stretch="Uniform"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Top"
                           Margin="0,5,20,0"
                           RenderTransformOrigin="0.5,0.5"
                           Panel.ZIndex="2"
                           MouseLeftButtonDown="GameElement_MouseLeftButtonDown">
                        <Image.RenderTransform>
                            <TranslateTransform x:Name="MonsterShakeTransform"/>
                        </Image.RenderTransform>
                    </Image>

                    <!-- 데미지 팝업 영역 -->
                    <Canvas x:Name="DamagePopupCanvas"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Width="100" Height="100"
                            Margin="0,5,5,0"
                            Panel.ZIndex="10"
                            IsHitTestVisible="False"/>

                    <!-- 히어로 위 플로팅 정보 (골드 Only) -->
                    <Border x:Name="GoldInfoBar"
                            Background="#CC000000" CornerRadius="12"
                            Padding="10,3"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Margin="5,0,0,100"
                            Panel.ZIndex="10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="GoldText" Text="💰 0" FontSize="11" Foreground="#FFD700" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>

                    <!-- 몬스터 아래 플로팅 정보 (타이머 Only) - 삭제됨 (Row 0로 이동) -->

                    <!-- 플레이어 히어로 (좌하단) -->
                    <Image x:Name="HeroImage"
                           Source="pack://application:,,,/Assets/Images/hero.png"
                           Width="120" Height="120"
                           Stretch="Uniform"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Bottom"
                           Margin="20,0,0,0"
                           Panel.ZIndex="3"
                           MouseLeftButtonDown="GameElement_MouseLeftButtonDown">
                        <Image.Clip>
                            <RectangleGeometry x:Name="HeroClip" Rect="0,0,120,100"/>
                        </Image.Clip>
                    </Image>
                </Grid>

                <!-- Row 2: 공격력 정보바 (관리 모드에서만 표시) -->
                <Border x:Name="PowerInfoBar" Grid.Row="2"
                        Background="#44000000" CornerRadius="5"
                        Padding="8,4" Margin="0,5,0,0"
                        Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" x:Name="KeyboardPowerText"
                                   Text="⌨️ Atk: 1" FontSize="10" Foreground="#88FFFF"
                                   HorizontalAlignment="Left"/>
                        <Border Grid.Column="1" Width="1" Background="#444" Margin="8,0"/>
                        <TextBlock Grid.Column="2" x:Name="MousePowerText"
                                   Text="🖱️ Atk: 1" FontSize="10" Foreground="#FF88FF"
                                   HorizontalAlignment="Right"/>
                    </Grid>
                </Border>

                <!-- Row 3: 업그레이드 패널 (관리 모드에서만 표시) -->
                <Border x:Name="UpgradePanel" Grid.Row="3"
                        Background="#88000000" CornerRadius="5"
                        Margin="0,5,0,0" Padding="5"
                        Visibility="Collapsed">
                    <StackPanel>
                        <!-- 업그레이드 버튼 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- 키보드 공격력 업그레이드 -->
                            <Button x:Name="UpgradeKeyboardBtn" Grid.Column="0"
                                    Click="UpgradeKeyboard_Click"
                                    Style="{StaticResource KeyboardUpgradeButton}"
                                    Margin="2" Padding="5,3"
                                    ToolTip="키보드 공격력 업그레이드">
                                <StackPanel>
                                    <TextBlock x:Name="UpgradeKeyboardText" Text="⌨️ UP" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="KeyboardCostText" Text="💰 100" FontSize="8"
                                               HorizontalAlignment="Center" Foreground="#FFD700"/>
                                </StackPanel>
                            </Button>

                            <!-- 마우스 공격력 업그레이드 -->
                            <Button x:Name="UpgradeMouseBtn" Grid.Column="1"
                                    Click="UpgradeMouse_Click"
                                    Style="{StaticResource MouseUpgradeButton}"
                                    Margin="2" Padding="5,3"
                                    ToolTip="마우스 공격력 업그레이드">
                                <StackPanel>
                                    <TextBlock x:Name="UpgradeMouseText" Text="🖱️ UP" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="MouseCostText" Text="💰 100" FontSize="8"
                                               HorizontalAlignment="Center" Foreground="#FFD700"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                        <!-- 유틸리티 버튼 -->
                        <Grid Margin="0,4,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- 통계 버튼 -->
                            <Button x:Name="StatsBtn" Grid.Column="0" Content="📊 Stats"
                                    Click="StatsButton_Click"
                                    Style="{StaticResource SettingsButton}"
                                    Margin="2,0" Height="20" FontSize="9"
                                    ToolTip="통계 보기"/>

                            <!-- 설정 버튼 -->
                            <Button x:Name="SettingsBtn" Grid.Column="1" Content="⚙️ Settings"
                                    Click="SettingsButton_Click"
                                    Style="{StaticResource SettingsButton}"
                                    Margin="2,0" Height="20" FontSize="9"
                                    ToolTip="설정 열기"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- 디버그 표시 -->
        <TextBlock x:Name="DebugText" Text="" Foreground="#888" FontSize="8"
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5"
                   Visibility="Collapsed"/>

        <!-- 입력 횟수 (숨김, 통계용) -->
        <TextBlock x:Name="InputCountText" Text="0" Visibility="Collapsed"/>

        <!-- Game Over Overlay -->
        <Border x:Name="GameOverOverlay" Visibility="Collapsed" Panel.ZIndex="200"
                Background="#EE000000" MouseLeftButtonDown="Window_MouseLeftButtonDown">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock x:Name="GameOverTitleText" Text="GENERATION ENDED" Foreground="#FF4444" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Red" BlurRadius="10" ShadowDepth="0"/>
                    </TextBlock.Effect>
                </TextBlock>

                <TextBlock x:Name="GameOverMessageText" Text="..." Foreground="#AAA" FontSize="10"
                           HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,20" TextWrapping="Wrap" MaxWidth="200"/>

                <Border Background="#33000000" CornerRadius="5" Padding="15" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" x:Name="ReportLevelLabel" Text="Max Level:" Foreground="#AAA" Margin="0,0,15,8"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="ReportLevelText" Text="0" Foreground="White" FontWeight="Bold"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" x:Name="ReportGoldLabel" Text="Gold Earned:" Foreground="#AAA" Margin="0,0,15,8"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="ReportGoldText" Text="0" Foreground="Gold" FontWeight="Bold"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" x:Name="ReportDamageLabel" Text="Damage Dealt:" Foreground="#AAA" Margin="0,0,15,8"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="ReportDamageText" Text="0" Foreground="#FF8888" FontWeight="Bold"/>
                    </Grid>
                </Border>

                <Button x:Name="CloseOverlayButton" Content="닫기" Click="CloseOverlayButton_Click"
                        Width="100" Height="30" Background="#333" Foreground="White" BorderBrush="#666" BorderThickness="1" Margin="0,0,0,10">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <TextBlock x:Name="AutoCloseCountdownText" Text="10초후 닫힘"
                           Foreground="#888" FontSize="10" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
