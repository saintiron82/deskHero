<Window x:Class="DeskWarrior.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DeskWarrior.ViewModels"
        Title="DeskWarrior"
        Width="280"
        Height="250"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Left="100"
        Top="100"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        KeyDown="Window_KeyDown">

    <Grid>
        <!-- 관리 모드 표시 테두리 -->
        <Border x:Name="ManageModeBorder"
                BorderBrush="#FFD700"
                BorderThickness="3"
                CornerRadius="10"
                Visibility="Collapsed"
                Panel.ZIndex="100"/>

        <!-- 모드 전환 버튼 (호버 시 표시) -->
        <Border x:Name="ModeToggleBorder"
                Background="#DD222233"
                BorderBrush="#666" BorderThickness="1"
                CornerRadius="6"
                Width="28" Height="28"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="8,8,0,0"
                Cursor="Hand"
                Opacity="0"
                ToolTip="관리 모드로 전환 (F1)"
                MouseLeftButtonDown="ModeToggle_MouseLeftButtonDown"
                Panel.ZIndex="150">
            <TextBlock x:Name="ModeIcon"
                       Text="👁️"
                       FontSize="16"
                       Foreground="#00CED1"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <!-- 종료 버튼 (관리 모드에서만 표시) -->
        <Border x:Name="ExitButtonBorder"
                Background="#662222"
                BorderBrush="#FF4444" BorderThickness="1"
                CornerRadius="6"
                Width="28" Height="28"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,8,8,0"
                Cursor="Hand"
                Visibility="Collapsed"
                ToolTip="종료"
                MouseLeftButtonDown="ExitButton_MouseLeftButtonDown"
                Panel.ZIndex="150">
            <TextBlock Text="❌"
                       FontSize="14"
                       Foreground="#FF8888"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <!-- 메인 컨텐츠 -->
        <Border x:Name="MainBackgroundBorder" Background="#EE1a1a2e" CornerRadius="10" Margin="3">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0: 적 정보 (우측 정렬) -->
                <Border x:Name="EnemyInfoBorder" Grid.Row="0"
                        Background="#44000000" CornerRadius="8" Padding="8,4"
                        HorizontalAlignment="Right">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="LevelText" Text="Lv.1" FontSize="12" Foreground="White" FontWeight="Bold"/>
                            <TextBlock Text="{Binding MonsterName}" FontSize="12" Foreground="#88ff88" Margin="5,0,0,0" FontWeight="SemiBold"/>
                            <TextBlock x:Name="MaxLevelText" Text=" (Best: 1)" FontSize="8" Foreground="#AAA" VerticalAlignment="Bottom" Margin="4,0,0,0" Visibility="Collapsed"/>
                            <TextBlock x:Name="MonsterEmoji" Text=" 👹" FontSize="12"/>
                        </StackPanel>
                        <!-- HP 바 -->
                        <Grid Height="6" Width="80" Margin="0,3,0,0">
                            <Border Background="#333" CornerRadius="3"/>
                            <Border x:Name="HpBar" Background="#00FF00" CornerRadius="3" HorizontalAlignment="Left" Width="80"/>
                        </Grid>
                        <TextBlock x:Name="HpText" Text="100/100" FontSize="9" Foreground="#AAA" HorizontalAlignment="Right" Visibility="Collapsed"/>
                    </StackPanel>
                </Border>

                <!-- Row 0: 타이머 (좌측 정렬) - UX 개선: 헤더 영역에 배치하여 시인성 확보 -->
                <Border x:Name="TimerInfoBar" Grid.Row="0"
                        Background="#44000000" CornerRadius="8" Padding="10,4"
                        HorizontalAlignment="Left" VerticalAlignment="Top"
                        Margin="40,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⏱️" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock Text=" " FontSize="12"/>
                        <TextBlock x:Name="TimerText" Text="30" FontSize="14" FontWeight="Bold" Foreground="#87CEEB" VerticalAlignment="Center" Width="25" TextAlignment="Center"/>
                    </StackPanel>
                </Border>

                    <!-- 배틀 필드 -->
                <Grid Grid.Row="1">
                    <!-- 배경 이미지 (가장 뒤) -->
                    <Image Source="pack://application:,,,/Assets/Images/bg_floor_sparse_pilot_v2.png"
                           Stretch="UniformToFill"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Top"
                           Height="150"
                           Margin="0,0,0,0"
                           Panel.ZIndex="1"/>

                    <!-- 적 몬스터 (우상단) -->
                    <Image x:Name="MonsterImage"
                           Source="pack://application:,,,/Assets/Images/monster.png"
                           Width="100" Height="100"
                           Stretch="Uniform"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Top"
                           Margin="0,5,20,0"
                           RenderTransformOrigin="0.5,0.5"
                           Panel.ZIndex="2"
                           MouseLeftButtonDown="GameElement_MouseLeftButtonDown">
                        <Image.RenderTransform>
                            <TranslateTransform x:Name="MonsterShakeTransform"/>
                        </Image.RenderTransform>
                    </Image>

                    <!-- 데미지 팝업 영역 -->
                    <Canvas x:Name="DamagePopupCanvas"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Width="100" Height="100"
                            Margin="0,5,5,0"
                            Panel.ZIndex="10"
                            IsHitTestVisible="False"/>

                    <!-- 히어로 위 플로팅 정보 (골드 + 크리스탈) -->
                    <Border x:Name="GoldInfoBar"
                            Background="#CC000000" CornerRadius="12"
                            Padding="10,3"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Margin="5,0,0,100"
                            Panel.ZIndex="10">
                        <StackPanel>
                            <TextBlock x:Name="GoldText" Text="💰 0" FontSize="11" Foreground="#FFD700" FontWeight="SemiBold"/>
                            <TextBlock x:Name="CrystalText" Text="💎 0" FontSize="11" Foreground="#00CED1" FontWeight="SemiBold" Margin="0,2,0,0">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="#00CED1" ShadowDepth="0" BlurRadius="8" Opacity="0.6"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- 몬스터 아래 플로팅 정보 (타이머 Only) - 삭제됨 (Row 0로 이동) -->

                    <!-- 플레이어 히어로 (좌하단) -->
                    <Image x:Name="HeroImage"
                           Source="pack://application:,,,/Assets/Images/hero.png"
                           Width="120" Height="120"
                           Stretch="Uniform"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Bottom"
                           Margin="20,0,0,0"
                           Panel.ZIndex="3"
                           MouseLeftButtonDown="GameElement_MouseLeftButtonDown">
                        <Image.Clip>
                            <RectangleGeometry x:Name="HeroClip" Rect="0,0,120,100"/>
                        </Image.Clip>
                    </Image>
                </Grid>

                <!-- Row 2: 공격력 정보바 (관리 모드에서만 표시) -->
                <Border x:Name="PowerInfoBar" Grid.Row="2"
                        Background="#44000000" CornerRadius="5"
                        Padding="8,4" Margin="0,5,0,0"
                        Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" x:Name="KeyboardPowerText"
                                   Text="⌨️ Atk: 1" FontSize="10" Foreground="#88FFFF"
                                   HorizontalAlignment="Left"/>
                        <Border Grid.Column="1" Width="1" Background="#444" Margin="8,0"/>
                        <TextBlock Grid.Column="2" x:Name="MousePowerText"
                                   Text="🖱️ Atk: 1" FontSize="10" Foreground="#FF88FF"
                                   HorizontalAlignment="Right"/>
                    </Grid>
                </Border>

                <!-- Row 3: 업그레이드 패널 (관리 모드에서만 표시) - 4열 컴팩트 그리드 -->
                <Border x:Name="UpgradePanel" Grid.Row="3"
                        Background="#88000000" CornerRadius="5"
                        Margin="0,5,0,0" Padding="4"
                        Visibility="Collapsed">
                    <StackPanel>
                        <!-- 스탯 업그레이드 그리드 (4열) -->
                        <UniformGrid Columns="4" Rows="2">
                            <!-- 키보드 공격력 -->
                            <Button x:Name="UpgradeKeyboardBtn"
                                    Click="UpgradeKeyboard_Click"
                                    Style="{StaticResource StatUpgradeButton}"
                                    Margin="2" Height="40"
                                    ToolTip="키보드 공격력 증가">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="⌨️" FontSize="12" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="KeyboardLevelText" Text="Lv.0" FontSize="8" HorizontalAlignment="Center" Foreground="#AAA"/>
                                    <TextBlock x:Name="KeyboardCostText" Text="💰100" FontSize="7" HorizontalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>

                            <!-- 마우스 공격력 -->
                            <Button x:Name="UpgradeMouseBtn"
                                    Click="UpgradeMouse_Click"
                                    Style="{StaticResource StatUpgradeButton}"
                                    Margin="2" Height="40"
                                    ToolTip="마우스 공격력 증가">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="🖱️" FontSize="12" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="MouseLevelText" Text="Lv.0" FontSize="8" HorizontalAlignment="Center" Foreground="#AAA"/>
                                    <TextBlock x:Name="MouseCostText" Text="💰100" FontSize="7" HorizontalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>

                            <!-- 골드+ -->
                            <Button x:Name="UpgradeGoldFlatBtn"
                                    Click="UpgradeGoldFlat_Click"
                                    Style="{StaticResource StatUpgradeButton}"
                                    Margin="2" Height="40"
                                    ToolTip="골드 추가 획득">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="💰" FontSize="12" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="GoldFlatLevelText" Text="Lv.0" FontSize="8" HorizontalAlignment="Center" Foreground="#AAA"/>
                                    <TextBlock x:Name="GoldFlatCostText" Text="💰150" FontSize="7" HorizontalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>

                            <!-- 골드* -->
                            <Button x:Name="UpgradeGoldMultiBtn"
                                    Click="UpgradeGoldMulti_Click"
                                    Style="{StaticResource StatUpgradeButton}"
                                    Margin="2" Height="40"
                                    ToolTip="골드 배율 증가">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="🌟" FontSize="12" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="GoldMultiLevelText" Text="Lv.0" FontSize="8" HorizontalAlignment="Center" Foreground="#AAA"/>
                                    <TextBlock x:Name="GoldMultiCostText" Text="💰200" FontSize="7" HorizontalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>

                            <!-- 시간 도둑 -->
                            <Button x:Name="UpgradeTimeThiefBtn"
                                    Click="UpgradeTimeThief_Click"
                                    Style="{StaticResource StatUpgradeButton}"
                                    Margin="2" Height="40"
                                    ToolTip="처치 시 시간 획득">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="⏱️" FontSize="12" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="TimeThiefLevelText" Text="Lv.0" FontSize="8" HorizontalAlignment="Center" Foreground="#AAA"/>
                                    <TextBlock x:Name="TimeThiefCostText" Text="💰300" FontSize="7" HorizontalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>

                            <!-- 콤보 유연성 -->
                            <Button x:Name="UpgradeComboFlexBtn"
                                    Click="UpgradeComboFlex_Click"
                                    Style="{StaticResource StatUpgradeButton}"
                                    Margin="2" Height="40"
                                    ToolTip="콤보 유지 시간 증가">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="🎯" FontSize="12" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="ComboFlexLevelText" Text="Lv.0" FontSize="8" HorizontalAlignment="Center" Foreground="#AAA"/>
                                    <TextBlock x:Name="ComboFlexCostText" Text="💰250" FontSize="7" HorizontalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>

                            <!-- 콤보 데미지 -->
                            <Button x:Name="UpgradeComboDamageBtn"
                                    Click="UpgradeComboDamage_Click"
                                    Style="{StaticResource StatUpgradeButton}"
                                    Margin="2" Height="40"
                                    ToolTip="콤보 데미지 배율 증가">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="💥" FontSize="12" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="ComboDamageLevelText" Text="Lv.0" FontSize="8" HorizontalAlignment="Center" Foreground="#AAA"/>
                                    <TextBlock x:Name="ComboDamageCostText" Text="💰300" FontSize="7" HorizontalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>

                            <!-- 빈 슬롯 (7개 버튼, 8칸 그리드) -->
                            <Border Background="Transparent"/>
                        </UniformGrid>

                        <!-- 유틸리티 버튼 -->
                        <Grid Margin="0,4,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- 통계 버튼 -->
                            <Button x:Name="StatsBtn" Grid.Column="0" Content="📊"
                                    Click="StatsButton_Click"
                                    Style="{StaticResource SettingsButton}"
                                    Margin="2,0" Height="20" FontSize="11"
                                    ToolTip="통계 보기"/>

                            <!-- 영구 상점 버튼 -->
                            <Button x:Name="PermanentShopBtn" Grid.Column="1" Content="💎"
                                    Click="PermanentShopButton_Click"
                                    Style="{StaticResource SettingsButton}"
                                    Margin="2,0" Height="20" FontSize="11"
                                    ToolTip="영구 업그레이드 상점"/>

                            <!-- 설정 버튼 -->
                            <Button x:Name="SettingsBtn" Grid.Column="2" Content="⚙️"
                                    Click="SettingsButton_Click"
                                    Style="{StaticResource SettingsButton}"
                                    Margin="2,0" Height="20" FontSize="11"
                                    ToolTip="설정 열기"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- 디버그 표시 -->
        <TextBlock x:Name="DebugText" Text="" Foreground="#888" FontSize="8"
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5"
                   Visibility="Collapsed"/>

        <!-- 입력 횟수 (숨김, 통계용) -->
        <TextBlock x:Name="InputCountText" Text="0" Visibility="Collapsed"/>

        <!-- Game Over Overlay -->
        <Border x:Name="GameOverOverlay" Visibility="Collapsed" Panel.ZIndex="200"
                Background="#CC000000" MouseLeftButtonDown="Window_MouseLeftButtonDown">
            <Border Background="#1A1A1A"
                    BorderBrush="#00CED1"
                    BorderThickness="2"
                    CornerRadius="10"
                    Padding="30"
                    MaxWidth="400"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect Color="#00CED1" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel>
                    <!-- 제목 -->
                    <TextBlock Text="⚰️ GAME OVER"
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="#FF4444"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Red" BlurRadius="10" ShadowDepth="0"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <!-- 게임 오버 메시지 -->
                    <TextBlock x:Name="GameOverMessageText"
                               Text="..."
                               Foreground="#AAA"
                               FontSize="10"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               Margin="0,0,0,20"
                               TextWrapping="Wrap"
                               MaxWidth="300"/>

                    <!-- 세션 통계 -->
                    <TextBlock Text="📊 세션 결과"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="0,0,0,10"/>
                    <StackPanel Margin="10,0,0,20">
                        <TextBlock Foreground="#CCC">
                            <Run Text="레벨: "/>
                            <Run x:Name="ReportLevelText" Text="0" Foreground="White" FontWeight="Bold"/>
                        </TextBlock>
                        <TextBlock Foreground="#CCC" Margin="0,3,0,0">
                            <Run Text="몬스터: "/>
                            <Run x:Name="ReportKillsText" Text="0" Foreground="White" FontWeight="Bold"/>
                            <Run Text="마리"/>
                        </TextBlock>
                        <TextBlock Foreground="#CCC" Margin="0,3,0,0">
                            <Run Text="데미지: "/>
                            <Run x:Name="ReportDamageText" Text="0" Foreground="#FF8888" FontWeight="Bold"/>
                        </TextBlock>
                        <TextBlock Foreground="#CCC" Margin="0,3,0,0">
                            <Run Text="골드: "/>
                            <Run x:Name="ReportGoldText" Text="0" Foreground="#FFD700" FontWeight="Bold"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- 크리스탈 획득 섹션 -->
                    <TextBlock Text="✨ 크리스탈 획득"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#00CED1"
                               Margin="0,0,0,10">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#00CED1" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <Border Background="#0D1117"
                            BorderBrush="#00CED1"
                            BorderThickness="1"
                            CornerRadius="5"
                            Padding="15"
                            Margin="0,0,0,20">
                        <StackPanel>
                            <!-- 골드 변환 -->
                            <TextBlock Text="💰 골드 변환"
                                       Foreground="#FFD700"
                                       FontWeight="Bold"
                                       Margin="0,0,0,5"/>
                            <TextBlock Foreground="#888" FontSize="12">
                                <Run x:Name="SessionGoldRun" Text="0"/>
                                <Run Text=" ÷ 1000 = "/>
                                <Run Text="+"/>
                                <Run x:Name="ConvertedCrystalsRun" Text="0" FontSize="16" FontWeight="Bold" Foreground="#00CED1"/>
                                <Run Text=" 💎"/>
                            </TextBlock>

                            <!-- 보스 드롭 (있는 경우) -->
                            <TextBlock x:Name="BossDropLine"
                                       Visibility="Collapsed"
                                       Margin="0,8,0,0"
                                       Foreground="#CCC">
                                <Run Text="🏆 보스 드롭: +"/>
                                <Run x:Name="BossDropCrystalsRun" Text="0" FontWeight="Bold" Foreground="#00CED1"/>
                                <Run Text=" 💎"/>
                            </TextBlock>

                            <!-- 업적 보상 (있는 경우) -->
                            <TextBlock x:Name="AchievementRewardLine"
                                       Visibility="Collapsed"
                                       Margin="0,5,0,0"
                                       Foreground="#CCC">
                                <Run Text="⭐ 업적 보상: +"/>
                                <Run x:Name="AchievementCrystalsRun" Text="0" FontWeight="Bold" Foreground="#00CED1"/>
                                <Run Text=" 💎"/>
                            </TextBlock>

                            <!-- 구분선 -->
                            <Border Height="1" Background="#333" Margin="0,12,0,12"/>

                            <!-- 총합 -->
                            <TextBlock FontSize="16"
                                       HorizontalAlignment="Center"
                                       Foreground="White">
                                <Run Text="총 획득: +"/>
                                <Run x:Name="TotalCrystalsEarnedRun" Text="0" FontSize="20" FontWeight="Bold" Foreground="#00CED1"/>
                                <Run Text=" 💎"/>
                            </TextBlock>
                            <TextBlock FontSize="14"
                                       Foreground="#888"
                                       HorizontalAlignment="Center"
                                       Margin="0,5,0,0">
                                <Run Text="현재 보유: "/>
                                <Run x:Name="CurrentCrystalBalanceRun" Text="0" Foreground="#00CED1" FontWeight="Bold"/>
                                <Run Text=" 💎"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- 액션 버튼 -->
                    <UniformGrid Columns="2" Rows="1" HorizontalAlignment="Center">
                        <Button x:Name="ShopButton"
                                Content="🛒 상점 (S)"
                                Click="ShopButton_Click"
                                Width="130"
                                Height="50"
                                Margin="0,0,10,0"
                                Background="#1E3A8A"
                                Foreground="White"
                                BorderBrush="#00CED1"
                                BorderThickness="2"
                                FontSize="14"
                                FontWeight="Bold">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                            <Button.Effect>
                                <DropShadowEffect Color="#00CED1" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
                            </Button.Effect>
                        </Button>
                        <Button x:Name="CloseOverlayButton"
                                Content="▶️ 게임 (SPACE)"
                                Click="CloseOverlayButton_Click"
                                Width="150"
                                Height="50"
                                Background="#059669"
                                Foreground="White"
                                BorderBrush="#047857"
                                BorderThickness="2"
                                FontSize="14"
                                FontWeight="Bold">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </UniformGrid>

                    <!-- 자동 닫기 카운트다운 -->
                    <TextBlock x:Name="AutoCloseCountdownText"
                               Text="10초 후 닫힘"
                               Foreground="#666"
                               FontSize="10"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
