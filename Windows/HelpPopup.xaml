<Window x:Class="DeskWarrior.Windows.HelpPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="도움말"
        Width="400"
        Height="300"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">

    <Window.Resources>
        <!-- Close Button Style -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#66FF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF6666"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Width="32" Height="32">
                            <TextBlock Text="✕" FontSize="16" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#88FF4444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Main Container: 검은색 배경 (글래스의 베이스) -->
    <Border Background="#1A1A1A" CornerRadius="12" BorderBrush="Black" BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="40" ShadowDepth="0" Opacity="0.8"/>
        </Border.Effect>

        <Grid Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- Header -->
                <RowDefinition Height="*"/>     <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Header (Top) -->
            <Border Grid.Row="0" Background="#66666666" CornerRadius="8,8,0,0" Padding="12,10"
                    BorderBrush="#33FFFFFF" BorderThickness="1"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown" Cursor="SizeAll">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Title (Center) -->
                    <TextBlock Grid.Column="0" x:Name="TitleText" Text="❓ 도움말" FontSize="14" FontWeight="Bold"
                               Foreground="#0099CC" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                    <!-- Close Button (Right) -->
                    <Button Grid.Column="1" Style="{StaticResource CloseButtonStyle}"
                            Click="CloseButton_Click" ToolTip="닫기"/>
                </Grid>
            </Border>

            <!-- Content (Body) -->
            <Border Grid.Row="1" Background="#E8E8E8" CornerRadius="0,0,8,8" Padding="20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ContentText" TextWrapping="Wrap" FontSize="12"
                               Foreground="#2A2A2A" LineHeight="20"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</Window>
