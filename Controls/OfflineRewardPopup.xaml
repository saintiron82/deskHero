<UserControl x:Class="DeskWarrior.Controls.OfflineRewardPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="320" Height="140"
             Opacity="0">

    <UserControl.Resources>
        <Storyboard x:Key="ShowAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.4"/>
            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                           Storyboard.TargetProperty="ScaleX"
                           From="0.8" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                           Storyboard.TargetProperty="ScaleY"
                           From="0.8" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="HideAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="1" To="0" Duration="0:0:0.3"/>
        </Storyboard>
    </UserControl.Resources>

    <UserControl.RenderTransform>
        <ScaleTransform x:Name="ScaleTransform" CenterX="160" CenterY="70"/>
    </UserControl.RenderTransform>

    <Border Background="#EE1a1a2e" CornerRadius="12" BorderBrush="#4CAF50" BorderThickness="2">
        <Grid Margin="16,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="🌙" FontSize="20" Margin="0,0,8,0"/>
                <TextBlock x:Name="HeaderText" Text="WELCOME BACK!"
                          Foreground="#4CAF50"
                          FontSize="14"
                          FontWeight="Bold"
                          VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Message -->
            <TextBlock x:Name="MessageText" Grid.Row="1"
                      Text="You earned rewards while offline!"
                      Foreground="White"
                      FontSize="12"
                      TextWrapping="Wrap"
                      TextAlignment="Center"
                      Margin="0,8,0,12"/>

            <!-- Rewards -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                <Border Background="#2a2a3e" CornerRadius="6" Padding="12,6" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🪙" FontSize="16" Margin="0,0,6,0"/>
                        <TextBlock x:Name="GoldText" Text="+0" Foreground="#FFD700" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="#2a2a3e" CornerRadius="6" Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💎" FontSize="16" Margin="0,0,6,0"/>
                        <TextBlock x:Name="CrystalText" Text="+0" Foreground="#E040FB" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Claim Button -->
            <Button x:Name="ClaimButton" Grid.Row="3"
                   Content="CLAIM"
                   Background="#4CAF50"
                   Foreground="White"
                   FontWeight="Bold"
                   FontSize="12"
                   Padding="24,8"
                   BorderThickness="0"
                   Cursor="Hand"
                   HorizontalAlignment="Center"
                   Click="ClaimButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</UserControl>
