<Window x:Class="DeskWarrior.PermanentUpgradeShop"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DeskWarrior.ViewModels"
        Title="💎 상점"
        Width="610"
        Height="600"
        MinHeight="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Topmost="False"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- 🎨 COLOR PALETTE -->
        <!-- #1A1A1A - Frame Black (외부 프레임) -->
        <!-- #66666666 - Glass Gray (글래스모피즘 - 회색 40% 투명) -->
        <!-- #E8E8E8 - Panel Light (Body 배경) -->
        <!-- #2A2A2A - Text Dark (텍스트) -->
        <!-- #0099CC - Accent Blue (크리스탈/강조) -->
        <!-- #10B981 - Action Green (버튼) -->
        <!-- #CCCCCC - Gray Disabled (비활성) -->
        <!-- #33FFFFFF - Glass Border (글래스 테두리 - 흰색 20% 투명) -->

        <!-- Tab Button Style -->
        <Style x:Key="TabButton" TargetType="RadioButton">
            <Setter Property="Background" Value="#44FFFFFF"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#66FFFFFF"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#0099CC"/>
                    <Setter Property="BorderBrush" Value="#0099CC"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Upgrade Card Style -->
        <Style x:Key="CompactUpgradeCard" TargetType="Border">
            <Setter Property="Background" Value="#CC1A1A1A"/>
            <Setter Property="BorderBrush" Value="#44FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Width" Value="180"/>
            <Setter Property="MinHeight" Value="95"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#0099CC"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Buy Button - Affordable (Green) -->
        <Style x:Key="BuyButtonAffordable" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Buy Button - Unaffordable (Gray) -->
        <Style x:Key="BuyButtonUnaffordable" TargetType="Button">
            <Setter Property="Background" Value="#CCCCCC"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="IsEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Buy Button - Maxed (Blue) -->
        <Style x:Key="BuyButtonMaxed" TargetType="Button">
            <Setter Property="Background" Value="#0099CC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="IsEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Buy RepeatButton - Affordable (Green) - 연속 구매 지원 -->
        <Style x:Key="BuyRepeatButtonAffordable" TargetType="RepeatButton">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Delay" Value="400"/>
            <Setter Property="Interval" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#059669"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#047857"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <!-- Main Container: 검은색 배경 (글래스의 베이스) -->
    <Border Background="#551A1A1A" CornerRadius="15" BorderBrush="Black" BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="40" ShadowDepth="0" Opacity="0.8"/>
        </Border.Effect>

        <Grid Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>   <!-- Header -->
                <RowDefinition Height="Auto"/>   <!-- Tabs -->
                <RowDefinition Height="*"/>      <!-- Content (남은 공간 모두 사용) -->
                <RowDefinition Height="Auto"/>   <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- ⭐ HEADER: Glass Morphism (Top) -->
            <Border Grid.Row="0" Background="#CC666666" CornerRadius="10,10,0,0" Padding="12,10"
                    BorderBrush="#33FFFFFF" BorderThickness="1"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown" Cursor="SizeAll">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <!-- Help -->
                        <ColumnDefinition Width="*"/>
                        <!-- Title -->
                        <ColumnDefinition Width="Auto"/>
                        <!-- Currency -->
                        <ColumnDefinition Width="Auto"/>
                        <!-- Close -->
                    </Grid.ColumnDefinitions>

                    <!-- Help Button (Left) -->
                    <Button x:Name="HelpButton" Grid.Column="0" Width="32" Height="32" Margin="0,0,10,0"
                            Background="#44FFFFFF" BorderBrush="#33FFFFFF" BorderThickness="1"
                            Cursor="Hand"
                            Click="HelpButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="6">
                                    <TextBlock Text="❓" FontSize="16" Foreground="White"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#66FFFFFF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Title (Center) -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Image Source="{DynamicResource IconCrystal}" Width="28" Height="28" Margin="0,0,6,0"/>
                        <TextBlock x:Name="ShopTitleText" FontSize="16" FontWeight="Bold"
                                   Foreground="#0099CC" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Currency Display (Right, before Close) -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,10,0">
                        <Border Background="#0099CC" CornerRadius="6" Padding="8,4" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{DynamicResource IconCrystal}" Width="24" Height="24"
                                       VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <TextBlock x:Name="CurrentCrystalsText" Text="0" FontSize="14"
                                           FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <StackPanel VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Close Button (Right) -->
                    <Button x:Name="CloseButton" Grid.Column="3" Width="32" Height="32"
                            Background="#66FF4444" BorderBrush="#FF6666" BorderThickness="1"
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="6">
                                    <TextBlock Text="✕" FontSize="16" Foreground="White"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#88FF4444"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- 🎯 TABS: Glass Morphism -->
            <Border Grid.Row="1" Background="#CC666666" Padding="15,10"
                    BorderBrush="#33FFFFFF" BorderThickness="0,0,0,1">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="0" Opacity="0.2"/>
                </Border.Effect>
                <WrapPanel x:Name="TabPanel" Orientation="Horizontal" HorizontalAlignment="Center">
                    <!-- Tabs are generated dynamically from PermanentStats.json categories -->
                </WrapPanel>
            </Border>

            <!-- 📦 CONTENT: Light Background -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="15,15,15,15"
                          Background="#DD666666">
                <Grid x:Name="UpgradeGrid" HorizontalAlignment="Center">
                    <!-- Dynamically populated -->
                </Grid>
            </ScrollViewer>

            <!-- 🔽 FOOTER: Glass Morphism -->
            <Border Grid.Row="3" Background="#CC666666" CornerRadius="0,0,10,10" Padding="15,10"
                    BorderBrush="#33FFFFFF" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>
                <!-- Hints -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Border Background="#44FFFFFF" CornerRadius="8" Padding="10,6"
                            BorderBrush="#33FFFFFF" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💡" FontSize="11" Margin="0,0,4,0"/>
                            <TextBlock x:Name="HintText" FontSize="11"
                                       Foreground="#FFFFFF" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
