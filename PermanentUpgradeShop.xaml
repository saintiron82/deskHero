<Window x:Class="DeskWarrior.PermanentUpgradeShop"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DeskWarrior.ViewModels"
        Title="💎 영구 업그레이드 상점"
        Width="900"
        Height="700"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Topmost="False"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- 버튼 기본 스타일 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3A3A3A"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1A1A1A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 탭 버튼 스타일 -->
        <Style x:Key="TabButton" TargetType="RadioButton">
            <Setter Property="Background" Value="#21262D"/>
            <Setter Property="Foreground" Value="#8B949E"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8,8,0,0"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D333B"/>
                    <Setter Property="Foreground" Value="#C9D1D9"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#0D1117"/>
                    <Setter Property="Foreground" Value="#58A6FF"/>
                    <Setter Property="BorderBrush" Value="#58A6FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 업그레이드 카드 스타일 -->
        <Style x:Key="UpgradeCard" TargetType="Border">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#252525"/>
                    <Setter Property="BorderBrush" Value="#00CED1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 컴팩트 업그레이드 카드 스타일 -->
        <Style x:Key="CompactUpgradeCard" TargetType="Border">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="140"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#252525"/>
                    <Setter Property="BorderBrush" Value="#00CED1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 업그레이드 버튼 - Affordable -->
        <Style x:Key="BuyButtonAffordable" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#059669"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#10B981"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#047857"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 업그레이드 버튼 - Unaffordable -->
        <Style x:Key="BuyButtonUnaffordable" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#4B5563"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="IsEnabled" Value="False"/>
        </Style>

        <!-- 업그레이드 버튼 - Maxed -->
        <Style x:Key="BuyButtonMaxed" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#1E40AF"/>
            <Setter Property="Foreground" Value="#60A5FA"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="IsEnabled" Value="False"/>
        </Style>

        <!-- 카테고리 헤더 스타일 -->
        <Style x:Key="CategoryHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00CED1"/>
            <Setter Property="Margin" Value="5,10,5,5"/>
        </Style>

        <!-- 업그레이드 카드 DataTemplate -->
        <DataTemplate x:Key="UpgradeCardTemplate">
            <Border Style="{StaticResource UpgradeCard}">
                <StackPanel>
                    <Grid Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Icon}" FontSize="20" Margin="0,0,8,0"/>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding LevelDisplay}" FontSize="9" Foreground="#6E7681"/>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock Text="{Binding CurrentEffect}" FontSize="14" FontWeight="Bold" Foreground="#FFD700" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>

                    <TextBlock Text="{Binding Description}" FontSize="9" Foreground="#8B949E" TextWrapping="Wrap" Margin="0,0,0,8"/>

                    <Border Background="#0D1117" CornerRadius="5" Padding="8,5" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock Text="다음 레벨 효과" FontSize="8" Foreground="#6E7681"/>
                            <TextBlock Text="{Binding NextLevelEffect}" FontSize="11" FontWeight="SemiBold" Foreground="#10B981"/>
                        </StackPanel>
                    </Border>

                    <Button Click="BuyUpgrade_Click" Tag="{Binding Id}" Height="32">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMaxed}" Value="True">
                                        <Setter Property="Background" Value="#1E40AF"/>
                                        <Setter Property="Foreground" Value="#60A5FA"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsMaxed}" Value="False"/>
                                            <Condition Binding="{Binding CanAfford}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Background" Value="#059669"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsMaxed}" Value="False"/>
                                            <Condition Binding="{Binding CanAfford}" Value="False"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Background" Value="#4B5563"/>
                                        <Setter Property="Foreground" Value="#9CA3AF"/>
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="{Binding ButtonText}" FontSize="11"/>
                    </Button>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Border Background="#0D1117" CornerRadius="15" BorderBrush="#30363D" BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.8"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="Auto"/>
                <!-- Tabs -->
                <RowDefinition Height="*"/>
                <!-- Content -->
                <RowDefinition Height="Auto"/>
                <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- ========================================
                 HEADER: Title & Currency Display
                 ======================================== -->
            <Border Grid.Row="0" Background="#161B22" CornerRadius="15,15,0,0" Padding="25,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: Title -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="💎 영구 업그레이드 상점" FontSize="24" FontWeight="Bold" Foreground="White">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#00CED1" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock Text="크리스탈을 사용하여 영구적인 능력을 강화하세요"
                                   FontSize="11" Foreground="#8B949E" Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Right: Currency Panel -->
                    <Border Grid.Column="1" Background="#0D1117" CornerRadius="10" Padding="20,12" BorderBrush="#30363D" BorderThickness="1">
                        <StackPanel>
                            <!-- Current Crystals -->
                            <Border Background="#1E40AF" CornerRadius="8" Padding="15,8" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="💎" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="보유 크리스탈" FontSize="9" Foreground="#93C5FD"/>
                                        <TextBlock x:Name="CurrentCrystalsText" Text="0" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Lifetime Stats -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Total Earned -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="총 획득" FontSize="8" Foreground="#6E7681" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="LifetimeEarnedText" Text="0" FontSize="11" FontWeight="SemiBold" Foreground="#FFD700" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <!-- Separator -->
                                <Border Grid.Column="1" Width="1" Background="#30363D"/>

                                <!-- Total Spent -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="총 사용" FontSize="8" Foreground="#6E7681" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="LifetimeSpentText" Text="0" FontSize="11" FontWeight="SemiBold" Foreground="#8B949E" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- ========================================
                 TABS: Category Selection
                 ======================================== -->
            <Border Grid.Row="1" Background="#21262D" Padding="25,10,25,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <RadioButton x:Name="TabBaseStats" Style="{StaticResource TabButton}" Margin="0,0,5,0" IsChecked="True" Checked="Tab_Checked">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚔️ 기본 능력"/>
                            <TextBlock x:Name="BadgeBaseStats" Text="" FontSize="10" FontWeight="Bold" Foreground="#10B981" Margin="5,0,0,0"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="TabCurrencyBonus" Style="{StaticResource TabButton}" Margin="0,0,5,0" Checked="Tab_Checked">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💰 재화 보너스"/>
                            <TextBlock x:Name="BadgeCurrencyBonus" Text="" FontSize="10" FontWeight="Bold" Foreground="#10B981" Margin="5,0,0,0"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="TabUtility" Style="{StaticResource TabButton}" Margin="0,0,5,0" Checked="Tab_Checked">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙️ 유틸리티"/>
                            <TextBlock x:Name="BadgeUtility" Text="" FontSize="10" FontWeight="Bold" Foreground="#10B981" Margin="5,0,0,0"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="TabStartingBonus" Style="{StaticResource TabButton}" Checked="Tab_Checked">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🚀 시작 보너스"/>
                            <TextBlock x:Name="BadgeStartingBonus" Text="" FontSize="10" FontWeight="Bold" Foreground="#10B981" Margin="5,0,0,0"/>
                        </StackPanel>
                    </RadioButton>
                </StackPanel>
            </Border>

            <!-- ========================================
                 CONTENT: Upgrade Grid (Dynamic)
                 ======================================== -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="20,15">
                <Grid x:Name="UpgradeGrid">
                    <!-- This will be dynamically populated -->
                </Grid>
            </ScrollViewer>

            <!-- ========================================
                 FOOTER: Close Button & Hints
                 ======================================== -->
            <Border Grid.Row="3" Background="#161B22" CornerRadius="0,0,15,15" Padding="25,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: Hints -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#0D1117" CornerRadius="5" Padding="8,4" Margin="0,0,10,0">
                            <TextBlock Text="ESC 키를 눌러 닫기" FontSize="10" Foreground="#6E7681"/>
                        </Border>
                        <Border Background="#0D1117" CornerRadius="5" Padding="8,4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💡" FontSize="10" Margin="0,0,5,0"/>
                                <TextBlock Text="보스를 처치하면 크리스탈을 획득할 수 있습니다" FontSize="10" Foreground="#8B949E"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Right: Close Button -->
                    <Button Grid.Column="1" Content="✕ 닫기" Click="CloseButton_Click"
                            Style="{StaticResource ModernButton}"
                            Background="#58A6FF"
                            Width="120" Height="36" FontSize="12" FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                <Setter Property="Background" Value="#58A6FF"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#79C0FF"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#388BFD"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
